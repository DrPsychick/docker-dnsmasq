# Build on every PR; push only on master
name: Build & Push

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    name: Build
    uses: ./.github/workflows/reusable-build.yml
    with:
      alpine_versions: '["3.24", "3.23", "latest", "3", "edge"]'
